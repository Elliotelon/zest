## 🛠 시작하기 (Environment Setup)

이 프로젝트는 보안을 위해 서버 주소 및 API Key를 `.xcconfig` 파일로 관리하며, 해당 파일은 Git 추적에서 제외되어 있습니다. 프로젝트를 클론한 후 아래 순서에 따라 설정을 완료해야 정상적으로 빌드됩니다.

1. **설정 파일 생성**
   - `Configs/Config.sample` 파일을 복사하여 같은 위치에 아래 3개의 파일을 생성합니다.
     - `Development.xcconfig`
     - `Staging.xcconfig`
     - `Production.xcconfig`

2. **서버 주소 입력**
   각 파일 내 `SERVER_HOST` 변수에 해당 환경의 API 도메인 주소를 입력합니다.
   ```text
   SERVER_HOST = 실제주소입력 (example.com) ***** https:// 제외하고 입력할것. *****
